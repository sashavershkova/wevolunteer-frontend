// Single source of truth for icon choices used across the app.
// Import icons from here (not directly from 'lucide-react') so every
// component agrees on which icon represents which concept.
export {
  MapPin as LocationIcon,
  Calendar as DateIcon,
  Clock as TimeIcon,
  User as SpotsIcon,
  Bell as NotificationsIcon,
} from 'lucide-react'